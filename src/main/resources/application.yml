spring:
  application:
    name: socam-BE
  profiles:
    active: local

  # MySQL RDS
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db-instant-socam-1.c1cog6k8ae32.ap-northeast-2.rds.amazonaws.com:3306/socam?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: master        # RDS 생성 시 입력한 마스터 계정
    password: socam1234!    # 마스터 계정 비밀번호

  jpa:
    hibernate:
      ddl-auto: update      # (개발용) 테이블 자동 생성/갱신
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true       # 콘솔에 SQL 출력 (개발 중 확인용)
    open-in-view: false      # 권장 설정 (lazy 로딩 시 메모리 누수 방지)

logging:
  level:
    org.springframework.security: DEBUG
